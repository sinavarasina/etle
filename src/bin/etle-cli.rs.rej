diff a/src/bin/etle-cli.rs b/src/bin/etle-cli.rs	(rejected hunks)
@@ -44,10 +44,6 @@ enum Command {
         /// Root directory for the local .etle library state.
         #[arg(long, value_name = "DIR")]
         library_root: Option<PathBuf>,
-
-        /// Keep serving peers instead of exiting after one transfer.
-        #[arg(long)]
-        forever: bool,
     },
 
     /// Seed an already persisted share from the local .etle library.
@@ -66,6 +62,10 @@ enum Command {
         /// Root directory for the local .etle library state.
         #[arg(long, value_name = "DIR")]
         library_root: Option<PathBuf>,
+
+        /// Keep serving peers instead of exiting after one transfer.
+        #[arg(long)]
+        forever: bool,
     },
 
     /// List local shares stored in the .etle library.
